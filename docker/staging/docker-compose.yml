version: '2'


services:
  mga:
    image: 'docker.skywell/message_gateway_admin_web/mga:latest'
    container_name: message_gateway_api
    build:
      context: ../..
      dockerfile: ./Dockerfile
    restart: 'no'
    networks:
      - default
